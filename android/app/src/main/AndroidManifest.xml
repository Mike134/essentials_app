<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Geo Location field group's "Capture location" button. geolocator's
         own Android plugin manifest declares no permissions itself (checked
         directly, unlike mobile_scanner's CAMERA permission, which does),
         so these have to be declared here. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- android_alarm_manager_plus (alarm-based scheduling, see
         claude/essentials-v2-alarm-scheduling-design.md). Its own
         AndroidManifest.xml declares no components at all, so the
         service/receivers below have to be declared here per the
         package's own README.
         SCHEDULE_EXACT_ALARM: needed for both exact=true and
         alarmClock=true (AlarmManagerCompat.setAlarmClock, the app's
         current mode; see alarm_schedule_service.dart's own doc
         comment for why plain exact=true wasn't enough: even a real
         exact alarm is throttled by Android's own anti-abuse policy to
         roughly once every 9 minutes, a real problem once
         schedule_interval allows intervals as short as 5). Not
         auto-granted on Android 13+ (target SDK here is well above
         that); the user grants it once via a system Settings screen,
         requested through permission_handler's
         Permission.scheduleExactAlarm from HomeShell's own bootstrap,
         same one-time pattern as the existing MANAGE_EXTERNAL_STORAGE
         gate. alarm_schedule_service.dart checks the real granted status
         before every arm and falls back to the old plain inexact
         behavior if it's ever not granted, so a revoked/never-granted
         permission degrades gracefully. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:label="essentials_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- android_alarm_manager_plus's own required components. See the
             uses-permission comment above for why these are declared here
             rather than in the plugin's own manifest. RebootBroadcastReceiver
             starts disabled; the plugin itself flips it on/off natively
             (PackageManager.setComponentEnabledSetting) whenever an alarm is
             scheduled with rescheduleOnReboot=true, so no custom Dart-side
             boot-completed handling is needed at all. -->
        <service
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.AlarmBroadcastReceiver"
            android:exported="false" />
        <receiver
            android:name="dev.fluttercommunity.plus.androidalarmmanager.RebootBroadcastReceiver"
            android:enabled="false"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required for url_launcher (link fields, e.g. supplier.hyperlink,
             journal.link) to find a browser to open http(s) links with:
             Android 11+'s package visibility restrictions otherwise hide
             every app, including the browser, from this app's queries. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
