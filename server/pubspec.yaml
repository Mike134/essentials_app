name: server
description: essentials_app's own crdt_sync coordinator -- the record-level sync hub for MIKE-CU. Not a general-purpose service.
version: 1.0.0

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  crdt: ^5.1.3
  crdt_sync: ^1.0.10
  path: ^1.9.0
  sqlite_crdt: ^3.0.4
  uuid: ^4.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
