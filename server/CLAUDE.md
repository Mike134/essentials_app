See @../CLAUDE.md for full project context, decisions, and conventions.
That file is the single source of truth, in the repo root — this pointer
exists just so a Claude Code session opened directly at `server/` still
picks it up.
